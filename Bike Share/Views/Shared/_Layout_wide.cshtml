<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - RIT Bikeshare</title>
    <link href='https://fonts.googleapis.com/css?family=Source+Sans+Pro:200' rel='stylesheet' type='text/css'>
    <link href='https://fonts.googleapis.com/css?family=Source+Sans+Pro:300' rel='stylesheet' type='text/css'>
    <link href='https://fonts.googleapis.com/css?family=Source+Sans+Pro:600' rel='stylesheet' type='text/css'>
    @if (IsSectionDefined("mapsSection"))
    {
        @RenderSection("mapsSection");
    }
    @Styles.Render("~/Content/bootstrap")
    @Styles.Render("~/Content/less")
    <link href='~/Content/footer.css' rel='stylesheet' type='text/css'>
</head>
<body style="background-color:#eeeeee">
    @{ Html.RenderAction("HeaderPartial", "Home"); }
    <div>
        <div style="padding-top: 50px;">
            @{Html.RenderAction("AnnouncementPartial", "Home");}
            <div class="row">
                 @RenderBody()
            </div>
        </div>
    </div>

@Html.Partial("_FooterPartial")
    @RenderSection("scripts", required: false)
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @if (IsSectionDefined("lateScripts"))
    {
        @RenderSection("lateScripts");
    }

    <div class="modal" id="siteModal" role="dialog" aria-labelledby="siteModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-body">Loading</div>
            </div>
        </div>
    </div>
</body>
</html>